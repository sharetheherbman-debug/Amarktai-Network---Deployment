# Optional AI Dependencies
# Install with: pip install -r requirements-ai.txt
# Requires: requirements.txt installed first

# Fetch.ai uAgents Framework (experimental multi-agent system)
# Enable with ENABLE_UAGENTS=true in .env
uagents==0.12.0

# Fetch.ai Payment Agent (autonomous payment processing)
# Enable with PAYMENT_AGENT_ENABLED=true in .env
cosmpy==0.9.2

# Note: These packages have complex dependencies that may conflict with core packages
# Only install if you need Fetch.ai decentralized agent and payment features
# See: https://fetch.ai/docs for more information
