# Constraints - Version pins to prevent dependency conflicts
# These constraints are applied across all requirement files

# NumPy constraint (scipy requires <2.3)
numpy>=1.26.0,<2.3

# Transformers ecosystem constraints
huggingface_hub>=0.23.2,<1.0
tokenizers>=0.20,<0.21

# Packaging constraint (langchain-core requires <25)
packaging>=23.2,<25

# Protobuf constraint (Google APIs vs cosmpy conflict)
# Use >=3.19.5,<7.0 for Google AI stack
# Use >=4.21.6,<5.0 for cosmpy (Fetch.ai agents)
# Cannot use both simultaneously
protobuf>=3.19.5,<7.0

# Typing extensions (keep recent)
typing_extensions>=4.15.0

# React/Frontend (if managing from backend)
# react@18.2.0
# react-dom@18.2.0
# date-fns@^3.6.0 (for react-day-picker@8.10.1)
