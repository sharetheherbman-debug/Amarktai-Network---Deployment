# Agents Requirements - Fetch.ai uAgents and CosmPy
# Install with: pip install -r base.txt -r agents.txt
# WARNING: These packages have complex dependencies and may conflict with other packages

# Fetch.ai Agent Framework
uagents==0.12.0

# Fetch.ai Payment Agent (Cosmos SDK)
cosmpy==0.9.2

# Note: cosmpy requires protobuf<5.0 which conflicts with Google AI packages
# Only install if you need Fetch.ai decentralized agent and payment features
# See: https://fetch.ai/docs for more information
